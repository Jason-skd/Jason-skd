# 仓库 AI 治理

本文件是本仓库 AI 协作的常驻入口。治理内容由本仓库独立维护，不动态继承、跟踪或同步任何外部治理仓库。

## 治理层次

以下文件共同构成本仓库的通用治理基线：

- 本文件：常驻的授权、证据、工作区和交付边界；
- [`docs/governance/workflow.md`](docs/governance/workflow.md)：任务分类、调查、实施和交付流程；
- [`docs/governance/engineering.md`](docs/governance/engineering.md)：设计、实现、测试和完成标准。

[`docs/governance/repository.md`](docs/governance/repository.md) 记录本仓库独有的治理，不属于通用治理基线。任何涉及文件或 Git 状态变更的任务都必须读取。仓库特有规则默认只能补充或收紧基线；未经用户显式授权，不得删除、放宽、替换基线规则或引入与其冲突的约束。通用实施授权不能绕过仓库特有门禁。

通用治理基线默认保持不变。若工作中发现可能适用于所有仓库的候选规则，应在写入前向用户说明适用范围、理由和影响，并征询是否将其纳入基线。获得明确授权前，只能将其作为建议报告，不得顺带写入。

## 权威与证据

- 当前用户任务决定本次目标、范围、优先级和明确排除项。
- 当前分支、revision、代码、测试、生成产物和锁定依赖决定当前实现事实。
- 仓库契约和架构文档决定长期边界；计划文档描述目标，不证明目标已经实现。
- 官方规范、当前锁定版本的依赖源码和供应商文档决定外部行为。
- 历史记录、旧讨论和记忆只能提供调查线索，不能替代当前证据。
- 不得声称未实际观察到的行为、验证结果、远端状态或外部操作结果。

## 授权边界

- 咨询、解释、调查、诊断、评审和方案讨论默认只读。
- 只有明确的实现、修复或变更指令才授权修改文件或外部状态。
- 能由现有契约和代码确定的低风险工程选择由执行者自行完成。
- 出现彼此不兼容的用户可见行为、不可逆操作、公开协议破坏、产品边界选择、权威来源冲突或缺失的外部输入时，停止相关变更并请求用户决定。
- 默认写入范围仅限当前启动目录所属仓库。写入其他仓库或修改远端服务必须有明确授权。

## 范围与工作区

- 修改前确认仓库、checkout、branch、revision 和未提交状态。
- 先读取适用治理、相邻实现、调用方和测试，再开始编辑。
- 只修改完成任务所需的内容，不顺带进行无关重构、清理或格式化。
- 保留用户及其他贡献者的未提交和无关改动；遇到同文件交叉修改时先读取完整 diff 并在现状上兼容。
- 不使用破坏性 Git 操作、批量覆盖或隐式暂存来制造干净工作区。

## 执行与交付

- 实现类任务必须交付完整、可验证的当前需求，不能只停留在建议或半成品。
- 失败后先定位原因、修复并复测；确实需要外部输入或高代价决策时再上报。
- 验证范围应与风险和影响面匹配，先运行最接近改动的检查，再逐步扩大。
- 最终检查完整 diff，并报告实际完成的变更、实际运行的验证、未运行项及剩余风险。
- 不把人格、身份、私人关系、固定称呼、情绪表达或个人记忆机制写入仓库治理。
